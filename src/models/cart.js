class Cart {
    constructor(quantity, productPrice, productTitle, sum, prodWeight) {
        this.quantity = quantity;
        this.productPrice = productPrice;
        this.productTitle = productTitle;
        this.sum = sum;
        this.prodWeight = prodWeight;
    }
}

export default Cart;